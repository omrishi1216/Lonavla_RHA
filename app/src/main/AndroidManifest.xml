<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lonavlarha">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".about_us_2"
            android:parentActivityName=".first_screen_si"></activity>
        <activity android:name=".About_Us_Page"></activity>
        <activity android:name=".settings_page"
            android:parentActivityName=".first_screen_si"></activity>

        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value="com.example.lonavlarha.first_screen_si" />

        <activity android:name=".first_screen_si" />
        <activity android:name=".first_screen_wsi" />
        <activity android:name=".guest_signup" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>